-php test/bin/php
-lib nme
-cp src
--remap flash:native
-main test.Test

-cmd echo "----------------------------------------"
-cmd echo "Test PHP!"
-cmd echo "Clear server file..."
-cmd rm -rf ~/Sites/TestPHP
-cmd echo "Copy compiled files to server..."
-cmd cp -r test/bin/php ~/Sites/TestPHP
-cmd echo "Open in browser..."
-cmd open http://localhost/~$USER/TestPHP?q=123
-cmd echo "Done!"
-cmd echo "----------------------------------------"
